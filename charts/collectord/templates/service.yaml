apiVersion: v1
kind: Service
metadata:
  name: {{ include "collectord.fullname" . }}
  labels:
    {{- include "collectord.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "collectord.selectorLabels" . | nindent 4 }}
