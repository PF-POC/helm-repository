apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "collectord.fullname" . }}-test-connection"
  labels:
    {{- include "collectord.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "collectord.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
