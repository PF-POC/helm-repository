{{ range $applicationTeams := .Values.applicationTeams -}}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $applicationTeams.teamName }}
  namespace: {{ $applicationTeams.teamName }}-team
  annotations:
    argocd.argoproj.io/sync-wave: {{ $app.syncWave | default $defaults.syncWave | quote }}
spec:
  destination:
    namespace: {{ $applicationTeams.teamName }}-team
    server: https://kubernetes.default.svc
  project: {{ $applicationTeams.teamName }}-project
  sources:
    - repoURL: {{ $applicationTeams.helmRepoUrl }}
      chart: {{ $applicationTeams.chart }}
      targetRevision: '{{ $applicationTeams.targetRevision }}'
      path: charts
      helm:
        valueFiles:
        - $values/{{ $applicationTeams.gitPath }}
        values: |
          teamName: {{ $applicationTeams.teamName }}
    - repoURL: {{ $applicationTeams.gitRepoUrl }}
      targetRevision: HEAD
      ref: values
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
    - ApplyOutOfSyncOnly=true
---
{{- end }}
